version: "3.8"

services:
  bot:
    build: .
    container_name: essencial-bot
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    env_file: .env
    environment:
      - NODE_ENV=production
      # RD Station Marketing — nomes exatos lidos pelo código (rdStationService.js)
      - RD_STATION_ENABLED=${RD_STATION_ENABLED:-false}
      - RD_STATION_API_KEY=${RD_STATION_API_KEY:-}
      - RD_STATION_CONVERSION_ID=${RD_STATION_CONVERSION_ID:-whatsapp-bot-essencial}
    volumes:
      - bot-logs:/app/logs
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  bot-logs:
    driver: local
